<h1>Alien Tracker</h1>

<ul>
  <% @aliens.each do |alien| %>
    <li>
      <p>
        [<%= alien.name %>]
        <br>
        <em><%= alien.description %></em>
      </p>
    </li>
  <% end %>
</ul>
